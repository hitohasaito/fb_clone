<h1>投稿一覧</h1>

<table>
  <tr>
    <th>タイトル</th>
    <th>内容</th>
  </tr>

<% @facebooks.each do |facebook| %>
<tr>
  <td><%= facebook.name %></td>
  <td><%= facebook.content %></td>
  <td><%= image_tag facebook.image.url %></td>
</tr>
<% end %>
</table>

<%= link_to "新規投稿画面に戻る",new_facebook_path %>
